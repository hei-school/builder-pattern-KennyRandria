class Car {
    constructor(seats, engine, tripComputer, gps, manual) {
      this.seats = seats;
      this.engine = engine;
      this.tripComputer = tripComputer;
      this.gps = gps;
      this.manual =manual
    }
  }
  
  export default Car;
  
